import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge paints over the bottom-left corner of every visual-QA capture.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
